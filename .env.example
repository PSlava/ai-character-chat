# ============================================
# AI Character Chat — Environment Variables
# ============================================
#
# Option 1 — Interactive setup (will ask for keys):
#   bash deploy/setup.sh
#
# Option 2 — Pre-fill and auto-install:
#   cp .env.example .env
#   nano .env                    # fill in API keys below
#   bash deploy/setup.sh --auto
#
# After setup, to change config:
#   nano .env
#   docker compose up -d
#
# ============================================

# --- Server ---
# Domain name for HTTPS. Leave empty if using IP only:
DOMAIN=
DEFAULT_MODEL=auto
# Frontend URL for email links (auto-detected by setup.sh):
FRONTEND_URL=

# --- Secrets (auto-generated by setup.sh if left as placeholders) ---
POSTGRES_PASSWORD=change-me-to-random-string
JWT_SECRET=change-me-to-another-random-string
WEBHOOK_SECRET=change-me-to-webhook-secret

# --- LLM API Keys (at least one required) ---
# Free providers (recommended):
GROQ_API_KEY=
CEREBRAS_API_KEY=
OPENROUTER_API_KEY=

# Direct API providers:
DEEPSEEK_API_KEY=
QWEN_API_KEY=
TOGETHER_API_KEY=

# Paid providers (optional):
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=

# --- Admin ---
# Comma-separated list of admin emails (get admin role on login/register):
ADMIN_EMAILS=

# --- Email (for password reset) ---
# Option A — Resend (recommended, https://resend.com):
RESEND_API_KEY=
RESEND_FROM_EMAIL=SweetSin <noreply@sweetsin.cc>
#
# Option B — SMTP (Gmail: smtp.gmail.com / 587 / your@gmail.com / App Password):
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
#
# Priority: Resend → SMTP → console (links printed to backend logs)

# --- Optional ---
PROXY_URL=
MAX_AVATAR_SIZE=4194304
